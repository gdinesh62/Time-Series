# Time-Series
 Implemented an Unsupervised Domain Adaptation (UDA) framework for time-series forecasting using  Domain Adversarial Neural Networks (DANN) to enable effective model generalization across distinct data  distributions without requiring target-domain labels. 
